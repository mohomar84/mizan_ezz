name: mizan
autocreate: true
image: okteto/maven:latest
command: bash
workdir: /okteto
volumes:
  - /root/.m2
sync:
  - .:/okteto
forward:
  - 5500:5500
resources:
  limits:
    cpu: "1"
    memory: 2Gi
